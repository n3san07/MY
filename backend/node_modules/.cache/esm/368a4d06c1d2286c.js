let mysql;_c1d‍.x([["con",()=>con]]);_c1d‍.w("mysql",[["default",["mysql"],function(v){mysql=v}]]);

       const con = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'my',
});

con.connect((err) => {
    if (err) {
        throw err;
    }

    console.log('DB Connected');
});
